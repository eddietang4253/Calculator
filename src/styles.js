import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    justifyContent: "flex-end"
  },
  value: {
    color: "#333",
    fontSize: 100,
    textAlign: "right",
    marginRight: 20,
    marginBottom: 10
  },
  row: {
    flexDirection: "row"
  }
});
